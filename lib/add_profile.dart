import 'package:flutter/material.dart';
import 'dart:io';
class AddProfileScreen extends StatelessWidget {
  const AddProfileScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text('Hello Jiii');
  }
}
